name: xmpp-platform
label: XMPP
kind: platform
version: 1.0.0
description: XMPP messaging platform for Hermes Agent.
requires_env:
  - XMPP_JID
  - name: XMPP_PASSWORD
    password: true
optional_env:
  - XMPP_ALLOWED_USERS
  - XMPP_STATE_PATH
  - XMPP_HOST
  - XMPP_PORT
  - XMPP_NICK
